apiVersion: batch/v1
kind: Job
metadata:
  name: jobname
spec:
  template:
    metadata:
      labels:
        app: gotest
    spec:
      containers:
      - name: counter
        image: centos:7
        command:
          - "bin/bash"
          - "-c"
          - "curl http://10.101.16.141:8080"
      restartPolicy: Never



