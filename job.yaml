apiVersion: batch/v1
kind: Job
metadata:
  name: jobname
spec:
  template:
    metadata:
      name: podname
    spec:
      containers:
      - name: counter
        image: centos:7
        command:
          - "curl http://127.0.0.1:8080 "
      restartPolicy: Never



